@model IEnumerable<Grouply.Models.Post>
@{
    ViewData["Title"] = "Welcome to Grouply";
}

<div class="max-w-2xl mx-auto text-center py-12">
    <h1 class="text-3xl font-bold mb-4">Welcome to Grouply 👋</h1>
    <p class="text-gray-600 mb-6">Join groups, share posts, and connect with people who share your interests.</p>

    <a href="/Identity/Account/Register" class="bg-blue-600 text-white px-6 py-3 rounded-full shadow">
        Sign Up
    </a>
    <a href="/Identity/Account/Login" class="ml-4 text-blue-600 underline">
        Log In
    </a>
</div>

<div class="max-w-3xl mx-auto mt-12 space-y-4">
    <h2 class="text-xl font-semibold mb-4">Popular Posts</h2>

    @foreach (var post in Model.Take(5))
    {
        @Html.Partial("~/Views/Shared/PostCard.cshtml", post)
    }
</div>